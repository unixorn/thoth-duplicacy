#!/usr/bin/env bash
#
# backup snapshots
#
# Copyright 2019, Joe Block <jpb@unixorn.net>

set -o pipefail

exec restic --repo "$BACKUP_REPOSITORY" \
  --exclude='.DS_Store' \
  --host $HOSTNAME \
  --verbose backup "/data/$BACKUP_LOCATION"
